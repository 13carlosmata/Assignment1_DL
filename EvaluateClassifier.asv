function P = EvaluateClassifier(X, W, b)
K=10;
%P = zeros(K,10000);
n=length(X);
P=[];
for i=1:n
    xp1=X(:,i);
    size(W)
    size(xp1)
    s =W*xp1 + b;
    P1=softmax(s);
    P=[P,P1];
end
 




